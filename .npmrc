# Use public npm registry
registry=https://registry.npmjs.org/


# Disable sending auth tokens to unintended registries
always-auth=false

# Prevent npm from using global configs if you want isolation
; prefix=${HOME}/.local/npm  # Uncomment to install global packages locally (optional)

# Optional: Silence update notices
; update-notifier=false

# Optional: Strict SSL (set to false if behind a corporate proxy causing SSL issues)
strict-ssl=true
